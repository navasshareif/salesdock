Installation Process:

1. 	Clone the branch
	git clone git@github.com:navasshareif/salesdock.git

2. 	Create a file .env and copy the content .env.example
	Change the DB configuration

3.	Run composer install

4.	Run php artisan key:generate

5.	Run php artisan migrate

6.	Run php artisan db:seed --class=ProductSeeder